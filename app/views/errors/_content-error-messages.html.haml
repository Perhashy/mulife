- if content.errors.any?
  - unless content.valid?
    - if errors = content.errors.full_messages_for(column.to_sym)
      %ul
        - errors.each do |error|
          %li{class: "error-message"}=error